{
  "name": "@jaystack/sls-sequelize-pg",
  "version": "0.0.0",
  "description": "AWS Serverless Sequelize PostgresDB utilities",
  "main": "dist/index.js",
  "module": "dist/esm/index.js",
  "typings": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "npm run clean && npm run compile",
    "clean": "rimraf -rf ./dist *.tsbuildinfo",
    "compile:cjs": "tsc -p tsconfig.build.json",
    "compile": "npm run compile:cjs",
    "prepublishOnly": "npm run build",
    "test": "jest --passWithNoTests"
  },
  "keywords": [],
  "author": "",
  "license": "UNLICENSED",
  "devDependencies": {
    "@types/jest": "^26.0.9",
    "@types/sequelize": "^4.28.9",
    "jest": "^26.2.2",
    "rimraf": "^3.0.2",
    "ts-jest": "^26.1.4",
    "typescript": "^3.9.7"
  },
  "dependencies": {
    "@jaystack/sls-core": "*",
    "debug": "^4.1.1",
    "sequelize": "^6.3.4"
  }
}
